/*program for Function Expression (anonymous) return to perform substraction */
// declaring a function
var calSub = function(x, y){
    let z = x - y;
    return z;
}

console.log("Subtraction : " + calSub(7, 4));